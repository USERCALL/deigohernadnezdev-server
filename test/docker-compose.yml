name: hello-world
services:
    hello-world:
        container_name: web-test
        ports:
            - 8081:8000
        image: crccheck/hello-world
# docker run -d --rm --name web-test -p 8081:8000 crccheck/hello-world
#
